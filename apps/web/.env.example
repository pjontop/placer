# Backend origin used by the frontend when calling the API
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080

# Development notes:
# - If you are testing cookies on localhost without TLS set COOKIE_SECURE=false
# - For cross-origin cookies set COOKIE_SAMESITE=None on backend and COOKIE_SECURE appropriately
